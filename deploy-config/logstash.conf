input {
  tcp {
    mode => "server"
    host => "0.0.0.0"
    port => 9500
    codec => json_lines
  }
}

filter {
  if [level] == "DEBUG" {
      drop { }
    }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "gateway-logs-%{+YYYY.MM.dd}"
  }
}